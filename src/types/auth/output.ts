export type OutputAuthMeType = {
    email: string,
    login: string,
    userId: string
}

export type RefreshTokenType = {
    refreshToken: string
}

export type AttemptsType = {
    ip: string,
    url: string,
    date: Date
}